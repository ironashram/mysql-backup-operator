
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: mysqlbackups.m1kcloud.m1k.cloud
spec:
  group: m1kcloud.m1k.cloud
  names:
    kind: MysqlBackup
    listKind: MysqlBackupList
    plural: mysqlbackups
    singular: mysqlbackup
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: MysqlBackup is the Schema for the mysqlbackups API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: MysqlBackupSpec defines the desired state of MysqlBackup
          properties:
            clusterRef:
              type: string
            executor:
              description: ExecutorSpec defines the desired backup method and scope
              properties:
                mysqldump:
                  description: TargetSpec defines the desired backup method and scope
                  properties:
                    databases:
                      items:
                        type: string
                      type: array
                  required:
                  - databases
                  type: object
              required:
              - mysqldump
              type: object
            storageRef:
              type: string
          required:
          - clusterRef
          - executor
          - storageRef
          type: object
        status:
          description: MysqlBackupStatus defines the observed state of MysqlBackup
          properties:
            BackupStatus:
              description: MysqlBackupStatusSpec defines the backup status
              properties:
                latestBackupStatus:
                  type: string
                performedBackups:
                  type: integer
              required:
              - latestBackupStatus
              - performedBackups
              type: object
          required:
          - BackupStatus
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
